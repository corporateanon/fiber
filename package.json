{
  "name": "fibers-playground",
  "version": "0.0.1",
  "main": "./lib/index.js",
  "license": "MIT",
  "dependencies": {
    "tslint": "^6.0.0",
    "typescript": "^3.8.3"
  },
  "files": [
    "./bin/*",
    "./lib/*"
  ],
  "typings": "./lib/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tslint -c tslint.json src/**/*.ts",
    "prepublish": "npm run build",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^25.1.3",
    "axios": "^0.19.2",
    "jest": "^25.1.0",
    "ts-jest": "^25.2.1"
  }
}
